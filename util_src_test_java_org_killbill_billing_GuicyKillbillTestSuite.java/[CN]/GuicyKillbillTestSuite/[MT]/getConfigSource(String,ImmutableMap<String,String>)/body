{
  try {
    return new TestKillbillConfigSource(file,DBTestingHelper.get().getInstance().getJdbcConnectionString(),DBTestingHelper.get().getInstance().getUsername(),DBTestingHelper.get().getInstance().getPassword(),extraProperties);
  }
 catch (  final Exception e) {
    final AssertionError assertionError=new AssertionError("Initialization error");
    assertionError.initCause(e);
    throw assertionError;
  }
}
