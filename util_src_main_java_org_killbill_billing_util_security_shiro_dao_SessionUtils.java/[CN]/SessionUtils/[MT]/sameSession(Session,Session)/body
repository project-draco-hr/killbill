{
  return (previousSession.getStartTimestamp() != null ? previousSession.getStartTimestamp().compareTo(newSession.getStartTimestamp()) == 0 : newSession.getStartTimestamp() == null) && (previousSession.getTimeout() == newSession.getTimeout()) && (previousSession.getHost() != null ? previousSession.getHost().equals(newSession.getHost()) : newSession.getHost() == null)&& sameSessionAttributes(previousSession,newSession);
}
