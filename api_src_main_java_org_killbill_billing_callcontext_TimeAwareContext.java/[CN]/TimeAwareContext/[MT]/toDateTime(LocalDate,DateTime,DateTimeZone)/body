{
  final LocalTime referenceLocalTime=toDateTime(referenceDateTime,accountTimeZone).toLocalTime();
  return new DateTime(localDate.getYear(),localDate.getMonthOfYear(),localDate.getDayOfMonth(),referenceLocalTime.getHourOfDay(),referenceLocalTime.getMinuteOfHour(),referenceLocalTime.getSecondOfMinute(),accountTimeZone);
}
