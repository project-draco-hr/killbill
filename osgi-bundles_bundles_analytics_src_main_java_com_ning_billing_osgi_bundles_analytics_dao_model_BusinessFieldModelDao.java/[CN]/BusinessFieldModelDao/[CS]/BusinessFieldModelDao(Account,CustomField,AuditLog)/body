{
  this(null,customField.getFieldName(),customField.getFieldValue(),customField.getCreatedDate(),creationAuditLog.getUserName(),creationAuditLog.getReasonCode(),creationAuditLog.getComment(),account.getId(),account.getName(),account.getExternalKey(),null,null);
}
