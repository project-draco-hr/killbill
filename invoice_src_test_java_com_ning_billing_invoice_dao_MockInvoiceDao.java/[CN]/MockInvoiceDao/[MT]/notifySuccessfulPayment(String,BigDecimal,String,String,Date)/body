{
synchronized (monitor) {
    invoicePayments.put(UUID.fromString(paymentAttemptId),new InvoicePayment(UUID.fromString(invoiceId),paymentAmount,Currency.valueOf(currency),UUID.fromString(paymentAttemptId),new DateTime(paymentAttemptDate)));
  }
}
