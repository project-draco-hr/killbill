{
  return paginationHelper.getPagination(AccountSqlDao.class,new PaginationIteratorBuilder<AccountModelDao,Account,AccountSqlDao>(){
    @Override public Iterator<AccountModelDao> build(    final AccountSqlDao accountSqlDao,    final Long limit){
      return accountSqlDao.searchAccounts(searchKey,offset,limit,context);
    }
  }
,offset,limit,context);
}
