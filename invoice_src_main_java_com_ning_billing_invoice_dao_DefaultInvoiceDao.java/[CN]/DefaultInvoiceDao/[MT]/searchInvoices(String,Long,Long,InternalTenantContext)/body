{
  return paginationHelper.getPagination(InvoiceSqlDao.class,new PaginationIteratorBuilder<InvoiceModelDao,Invoice,InvoiceSqlDao>(){
    @Override public Iterator<InvoiceModelDao> build(    final InvoiceSqlDao invoiceSqlDao,    final Long limit){
      return invoiceSqlDao.searchInvoices(searchKey,offset,limit,context);
    }
  }
,offset,limit,context);
}
