{
  this(bundleRecordId,bundle.getId(),bundle.getExternalKey(),bst.getSubscriptionId(),bundleAccountRank,bst,bundle.getCreatedDate(),creationAuditLog.getUserName(),creationAuditLog.getReasonCode(),creationAuditLog.getComment(),account.getId(),account.getName(),account.getExternalKey(),accountRecordId,tenantRecordId,reportGroup);
}
