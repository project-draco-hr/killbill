{
synchronized (this) {
    try {
      wait(timeout);
    }
 catch (    Exception ignore) {
    }
  }
  if (!completed) {
    log.warn("ApiTestListener did not complete in " + timeout + " ms");
  }
  return completed;
}
