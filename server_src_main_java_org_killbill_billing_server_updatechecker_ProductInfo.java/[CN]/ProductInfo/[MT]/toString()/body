{
  final String fullProductName=String.format("%s (%s)",getName(),isEnterprise() ? "enterprise" : "community");
  return String.format("%s version %s was built on %s, with jdk %s by %s",fullProductName,getVersion(),getBuildTime(),getBuildJdk(),getBuiltBy());
}
