{
  defaultPricelist.validate(catalog,errors);
  for (  PriceList pl : childPriceLists) {
    if (pl.getName().equals(IPriceListSet.DEFAULT_PRICELIST_NAME)) {
      errors.add(new ValidationError("Pricelists cannot use the reserved name '" + IPriceListSet.DEFAULT_PRICELIST_NAME + "'",catalog.getCatalogURI(),PriceListSet.class,pl.getName()));
    }
    pl.validate(catalog,errors);
  }
  return errors;
}
