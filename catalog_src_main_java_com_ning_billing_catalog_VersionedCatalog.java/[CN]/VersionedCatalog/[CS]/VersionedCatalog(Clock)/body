{
  this.clock=clock;
  StandaloneCatalog baseline=new StandaloneCatalog(new Date(0));
  try {
    add(baseline);
  }
 catch (  CatalogApiException e) {
    log.error("This error should never happpen",e);
  }
}
