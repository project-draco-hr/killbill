{
  if (getClass() == obj.getClass()) {
    PaymentProviderContactData other=(PaymentProviderContactData)obj;
    if (obj == other) {
      return true;
    }
 else {
      return Objects.equal(firstName,other.firstName) && Objects.equal(lastName,other.lastName) && Objects.equal(email,other.email)&& Objects.equal(phoneNumber,other.phoneNumber)&& Objects.equal(externalKey,other.externalKey);
    }
  }
  return false;
}
