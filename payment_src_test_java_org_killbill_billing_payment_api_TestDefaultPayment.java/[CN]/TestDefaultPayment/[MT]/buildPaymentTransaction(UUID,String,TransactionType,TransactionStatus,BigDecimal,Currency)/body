{
  return new DefaultPaymentTransaction(UUID.randomUUID(),UUID.randomUUID(),externalKey,clock.getUTCNow(),clock.getUTCNow(),paymentId,transactionType,clock.getUTCNow(),transactionStatus,amount,currency,amount,currency,null,null,null);
}
