{
  return new BusinessSubscriptionTransition(subscriptionId,totalOrdering,EXTERNAL_KEY,ACCOUNT_KEY,requestedTransitionTime,eventType,previousSubscription,nextState == null ? null : new BusinessSubscription(null,plan.getName(),phase.getName(),CURRENCY,effectiveTransitionTime,nextState,subscriptionId,bundleUUID,catalog));
}
