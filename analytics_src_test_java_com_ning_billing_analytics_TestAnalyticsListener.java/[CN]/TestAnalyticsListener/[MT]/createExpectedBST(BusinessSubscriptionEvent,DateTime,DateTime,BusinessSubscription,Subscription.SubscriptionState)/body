{
  return new BusinessSubscriptionTransition(KEY,ACCOUNT_KEY,requestedTransitionTime,eventType,previousSubscription,nextState == null ? null : new BusinessSubscription(null,plan,phase,CURRENCY,effectiveTransitionTime,nextState,subscriptionId,bundleUUID));
}
