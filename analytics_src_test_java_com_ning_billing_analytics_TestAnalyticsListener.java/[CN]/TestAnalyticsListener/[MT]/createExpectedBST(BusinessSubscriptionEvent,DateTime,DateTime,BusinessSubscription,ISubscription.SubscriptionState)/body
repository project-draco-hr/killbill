{
  return new BusinessSubscriptionTransition(KEY,ACCOUNT_KEY,requestedTransitionTime,eventType,previousSubscription,new BusinessSubscription(null,plan,phase,CURRENCY,effectiveTransitionTime,nextState,subscriptionId,bundleUUID));
}
