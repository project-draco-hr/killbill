{
  dbi.inTransaction(new TransactionCallback<Void>(){
    @Override public Void inTransaction(    Handle h,    TransactionStatus status) throws Exception {
      h.execute("create table dummy " + "(id int(11) unsigned NOT NULL AUTO_INCREMENT, " + "dummy_id char(36) NOT NULL, "+ "PRIMARY KEY(id)"+ ") ENGINE=innodb;");
      return null;
    }
  }
);
}
