{
  this(invoice.getAccountId(),accountKey,invoice.getChargedAmount(),invoice.getCreditedAmount(),invoice.getPaidAmount(),invoice.getBalance(),invoice.getCreatedDate(),invoice.getCurrency(),invoice.getInvoiceDate(),invoice.getId(),invoice.getInvoiceNumber(),invoice.getTargetDate(),invoice.getUpdatedDate());
}
