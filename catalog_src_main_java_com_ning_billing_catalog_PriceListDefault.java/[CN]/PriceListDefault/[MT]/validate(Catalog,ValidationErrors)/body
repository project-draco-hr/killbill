{
  if (getName().equals(IPriceListSet.DEFAULT_PRICELIST_NAME)) {
    errors.add(new ValidationError("The name of the default pricelist must be 'DEFAULT'",catalog.getCatalogURI(),PriceList.class,getName()));
  }
  return errors;
}
