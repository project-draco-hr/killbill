{
  this(null,subscriptionEvent.getTotalOrdering(),bundle.getId(),bundle.getExternalKey(),subscriptionEvent.getSubscriptionId(),requestedTimestamp,event,previousSubscription,nextSubscription,null,createdBy,createdReasonCode,createdComments,account.getId(),account.getName(),account.getExternalKey(),null,null);
}
