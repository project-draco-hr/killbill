{
  this(null,bundle.getId(),bundle.getExternalKey(),transition.getSubscriptionId(),requestedTimestamp,event,previousSubscription,nextSubscription,null,createdBy,createdReasonCode,createdComments,account.getId(),account.getName(),account.getExternalKey(),null,null);
}
