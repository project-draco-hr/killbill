{
  Handle handle=null;
  try {
    handle=dbi.open();
    final Call call=handle.createCall(storedProcedureName);
    call.invoke();
  }
  finally {
    if (handle != null) {
      handle.close();
    }
  }
}
