{
synchronized (events) {
    List<IEvent> results=new LinkedList<IEvent>();
    for (    IEvent cur : events) {
      if (cur.isActive() && cur.getProcessingState() == IEventLyfecycleState.AVAILABLE && cur.getSubscriptionId().equals(subscriptionId)) {
        results.add(cur);
      }
    }
    return results;
  }
}
