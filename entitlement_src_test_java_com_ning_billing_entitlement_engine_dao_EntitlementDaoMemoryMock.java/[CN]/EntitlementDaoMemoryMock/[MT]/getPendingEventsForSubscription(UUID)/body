{
synchronized (events) {
    List<IEntitlementEvent> results=new LinkedList<IEntitlementEvent>();
    for (    IEntitlementEvent cur : events) {
      if (cur.isActive() && cur.getProcessingState() == IEventLifecycleState.AVAILABLE && cur.getSubscriptionId().equals(subscriptionId)) {
        results.add(cur);
      }
    }
    return results;
  }
}
