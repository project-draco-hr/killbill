{
synchronized (this) {
    try {
      wait(timeout);
    }
 catch (    Exception ignore) {
    }
  }
  if (!completed) {
    log.debug("TestBusHandler did not complete in " + timeout + " ms");
  }
  return completed;
}
