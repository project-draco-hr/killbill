{
  for (  DefaultLimit limit : limits) {
    if (!limit.getUnit().getName().equals(unit)) {
      continue;
    }
    if (!limit.compliesWith(value)) {
      return false;
    }
  }
  return true;
}
