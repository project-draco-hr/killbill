{
  doCallAndCheckForCompletion(new Function<Void,Void>(){
    @Override public Void apply(    @Nullable final Void input){
      try {
        paymentApi.createChargebackWithPaymentControl(account,payment.getId(),payment.getPurchasedAmount(),payment.getCurrency(),UUID.randomUUID().toString(),PAYMENT_OPTIONS,refreshedCallContext());
      }
 catch (      PaymentApiException e) {
        fail(e.toString());
        return null;
      }
      return null;
    }
  }
,events);
}
