{
  doCallAndCheckForCompletion(new Function<Void,Void>(){
    @Override public Void apply(    @Nullable final Void input){
      try {
        paymentApi.createRefundWithAdjustment(account,payment.getId(),payment.getCapturedAmount(),PLUGIN_PROPERTIES,callContext);
      }
 catch (      final PaymentApiException e) {
        fail(e.toString());
      }
      return null;
    }
  }
,events);
}
