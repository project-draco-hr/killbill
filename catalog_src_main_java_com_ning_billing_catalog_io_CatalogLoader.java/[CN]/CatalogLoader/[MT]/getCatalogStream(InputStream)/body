{
  Object o=unmarshaller().unmarshal(stream);
  if (o instanceof Catalog) {
    Catalog c=(Catalog)o;
    c.setCatalogURL("embedded catalog");
    validate(c);
    return (Catalog)o;
  }
 else {
    return null;
  }
}
