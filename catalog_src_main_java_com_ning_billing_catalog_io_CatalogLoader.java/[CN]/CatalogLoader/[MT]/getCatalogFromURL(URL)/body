{
  Object o=unmarshaller().unmarshal(url);
  if (o instanceof Catalog) {
    Catalog c=(Catalog)o;
    c.setCatalogURL(url.toString());
    validate(c);
    return (Catalog)o;
  }
 else {
    return null;
  }
}
