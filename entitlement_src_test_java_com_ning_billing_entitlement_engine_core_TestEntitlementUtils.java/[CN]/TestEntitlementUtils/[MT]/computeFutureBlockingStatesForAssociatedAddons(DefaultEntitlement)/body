{
  return entitlementUtils.computeFutureBlockingStatesForAssociatedAddons(sqlDao.getBlockingHistoryForService(baseEntitlement.getId(),EntitlementService.ENTITLEMENT_SERVICE_NAME,internalCallContext),baseEntitlement.getSubscriptionBase(),clock.getUTCNow(),internalCallContext);
}
