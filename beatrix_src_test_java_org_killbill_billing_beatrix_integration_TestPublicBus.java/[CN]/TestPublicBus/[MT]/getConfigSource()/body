{
  ImmutableMap additionalProperties=new ImmutableMap.Builder().put("org.killbill.billing.util.broadcast.rate","500ms").build();
  return getConfigSource("/beatrix.properties",additionalProperties);
}
