{
  if (paymentControlPluginNames == null || paymentControlPluginNames.size() == 0) {
    return "";
  }
  final StringBuilder tmp=new StringBuilder(prefixMessage).append("(").append(JOINER.join(paymentControlPluginNames)).append(")");
  return tmp.toString();
}
