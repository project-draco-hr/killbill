{
  final HashBiMap<Integer,CategoryIdAndMetric> accumulator=HashBiMap.create();
  for (  final Map<String,Object> metricInfo : delegate.getMetrics()) {
    accumulator.put(Integer.valueOf(metricInfo.get("sample_kind_id").toString()),new CategoryIdAndMetric((Integer)metricInfo.get("event_category_id"),metricInfo.get("sample_kind").toString()));
  }
  return accumulator;
}
