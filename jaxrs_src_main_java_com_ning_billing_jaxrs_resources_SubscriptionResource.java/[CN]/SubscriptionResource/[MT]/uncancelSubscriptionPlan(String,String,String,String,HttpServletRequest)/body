{
  final UUID uuid=UUID.fromString(subscriptionId);
  final SubscriptionBase current=null;
  current.uncancel(context.createContext(createdBy,reason,comment,request));
  return Response.status(Status.OK).build();
}
