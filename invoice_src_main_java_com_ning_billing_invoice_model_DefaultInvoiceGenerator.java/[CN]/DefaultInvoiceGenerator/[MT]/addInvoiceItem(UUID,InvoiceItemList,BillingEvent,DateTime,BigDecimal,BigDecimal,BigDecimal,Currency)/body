{
  DefaultInvoiceItem item=new DefaultInvoiceItem(invoiceId,event.getSubscription().getId(),event.getPlan().getName(),event.getPlanPhase().getName(),event.getEffectiveDate(),billThroughDate,amount,rate,fixedAmount,currency);
  items.add(item);
}
