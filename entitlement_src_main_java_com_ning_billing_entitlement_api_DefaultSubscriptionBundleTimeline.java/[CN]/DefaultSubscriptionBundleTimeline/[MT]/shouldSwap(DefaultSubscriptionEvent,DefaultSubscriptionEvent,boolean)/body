{
  return (cur.getEffectiveDateTime().compareTo(other.getEffectiveDateTime()) == 0 && cur.getEntitlementId().equals(other.getEntitlementId()) && ((isAscending && cur.getSubscriptionEventType().ordinal() > other.getSubscriptionEventType().ordinal()) || (!isAscending && cur.getSubscriptionEventType().ordinal() < other.getSubscriptionEventType().ordinal())));
}
