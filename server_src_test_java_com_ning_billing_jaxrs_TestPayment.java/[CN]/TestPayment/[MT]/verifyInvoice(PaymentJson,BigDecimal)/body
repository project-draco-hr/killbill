{
  final InvoiceJsonSimple invoiceJsonSimple=getInvoice(paymentJson.getInvoiceId());
  Assert.assertEquals(invoiceJsonSimple.getBalance().setScale(2,BigDecimal.ROUND_HALF_UP),expectedInvoiceBalance.setScale(2,BigDecimal.ROUND_HALF_UP));
}
