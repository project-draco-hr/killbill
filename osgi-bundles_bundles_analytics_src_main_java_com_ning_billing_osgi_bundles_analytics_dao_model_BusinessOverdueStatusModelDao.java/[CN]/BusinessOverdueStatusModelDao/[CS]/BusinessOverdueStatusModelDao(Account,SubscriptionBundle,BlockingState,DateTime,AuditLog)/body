{
  this(null,subscriptionBundle.getId(),subscriptionBundle.getExternalKey(),blockingState.getStateName(),blockingState.getTimestamp(),endDate,blockingState.getCreatedDate(),creationAuditLog.getUserName(),creationAuditLog.getReasonCode(),creationAuditLog.getComment(),account.getId(),account.getName(),account.getExternalKey(),null,null);
}
