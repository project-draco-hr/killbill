{
  return new InternalCallContext(context.getTenantRecordId(),accountRecordId,context.getUserToken(),context.getCreatedBy(),context.getCallOrigin(),context.getUserType(),context.getReasonCode(),context.getComment(),context.getCreatedDate(),context.getUpdatedDate());
}
