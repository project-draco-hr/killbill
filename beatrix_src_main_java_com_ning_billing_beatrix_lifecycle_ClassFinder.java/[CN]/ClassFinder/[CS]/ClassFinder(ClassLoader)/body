{
  this.loader=loader;
  this.map=initialize();
  Iterator<String> it=map.keySet().iterator();
  while (it.hasNext()) {
    String key=it.next();
    log.info("Found classes - " + key + " : "+ map.get(key));
  }
}
