{
  this(event.getEventType().toString(),event.getAccountId() != null ? event.getAccountId().toString() : null,event.getObjectType().toString(),event.getObjectId() != null ? event.getObjectId().toString() : null);
}
