{
  final Account account=accountApi.getAccountById(UUID.fromString(accountId));
  invoiceApi.deleteCBA(account.getId(),UUID.fromString(invoiceId),UUID.fromString(invoiceItemId),context.createContext(createdBy,reason,comment));
  return Response.status(Status.OK).build();
}
