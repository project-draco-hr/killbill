{
  BigDecimal amountNegated=amount == null ? null : amount.negate();
  return new RecurringInvoiceItem(invoiceId,subscriptionId,bundleId,planName,phaseName,startDate,endDate,amountNegated,rate,currency,id,createdDate);
}
