{
  BigDecimal amountNegated=amount == null ? null : amount.negate();
  return new RecurringInvoiceItem(invoiceId,accountId,subscriptionId,planName,phaseName,startDate,endDate,amountNegated,rate,currency,id);
}
