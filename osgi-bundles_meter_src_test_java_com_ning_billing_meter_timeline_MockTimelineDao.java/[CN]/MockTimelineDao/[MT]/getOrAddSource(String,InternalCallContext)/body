{
synchronized (hosts) {
    final Integer hostId=getSourceId(host,context);
    if (hostId == null) {
      hosts.put(hosts.size() + 1,host);
      return hosts.size();
    }
 else {
      return hostId;
    }
  }
}
