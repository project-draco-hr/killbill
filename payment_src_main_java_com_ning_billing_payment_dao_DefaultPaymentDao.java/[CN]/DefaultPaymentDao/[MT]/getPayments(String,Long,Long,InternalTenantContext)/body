{
  return paginationHelper.getPagination(PaymentSqlDao.class,new PaginationIteratorBuilder<PaymentModelDao,Payment,PaymentSqlDao>(){
    @Override public Iterator<PaymentModelDao> build(    final PaymentSqlDao paymentSqlDao,    final Long limit){
      return paymentSqlDao.getByPluginName(pluginName,offset,limit,context);
    }
  }
,offset,limit,context);
}
