{
  if (property == null) {
    return null;
  }
  URI uri=(property.startsWith(URI_SCHEME_FOR_CLASSPATH)) ? new URI(property) : new File("src/test/resources/testInput.xml").toURI();
  return getObjectFromURI(uri,objectType);
}
