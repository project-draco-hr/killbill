{
  return Collections2.filter(events,new Predicate<SubscriptionEvent>(){
    @Override public boolean apply(    final SubscriptionEvent input){
      return !initialEvents.contains(input);
    }
  }
);
}
