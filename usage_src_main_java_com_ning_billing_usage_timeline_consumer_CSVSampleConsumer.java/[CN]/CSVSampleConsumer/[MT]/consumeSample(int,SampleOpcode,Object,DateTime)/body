{
  if (time != null) {
    final String valueString=value == null ? "0" : value.toString();
    if (builderSampleNumber > 0) {
      builder.append(",");
    }
    builder.append(DateTimeUtils.unixSeconds(time)).append(",").append(valueString);
    builderSampleNumber++;
  }
}
