{
  final DateTime createdDate=utcNow;
  final DateTime updatedDate=utcNow;
  return new DirectPaymentModelDao(createdDate,updatedDate,directPaymentStateContext.getAccount().getId(),directPaymentStateContext.getPaymentMethodId(),directPaymentStateContext.getDirectPaymentExternalKey());
}
