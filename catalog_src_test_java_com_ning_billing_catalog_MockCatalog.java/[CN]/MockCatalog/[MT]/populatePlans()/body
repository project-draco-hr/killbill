{
  Product[] products=getProducts();
  Plan[] plans=new Plan[products.length];
  for (int i=0; i < products.length; i++) {
    plans[i]=new Plan(products[i].getName().toLowerCase() + "-plan",products[i],new PlanPhase(BillingPeriod.MONTHLY,PhaseType.EVERGREEN));
  }
  setPlans(plans);
}
