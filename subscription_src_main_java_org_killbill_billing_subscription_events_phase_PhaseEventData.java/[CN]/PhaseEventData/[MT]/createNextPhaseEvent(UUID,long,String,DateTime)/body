{
  return (phaseName == null) ? null : new PhaseEventData(new PhaseEventBuilder().setSubscriptionId(subscriptionId).setEffectiveDate(effectiveDate).setActiveVersion(activeVersion).setPhaseName(phaseName));
}
