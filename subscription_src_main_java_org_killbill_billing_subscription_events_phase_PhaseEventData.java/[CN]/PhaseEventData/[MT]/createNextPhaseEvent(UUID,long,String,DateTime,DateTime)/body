{
  return (phaseName == null) ? null : new PhaseEventData(new PhaseEventBuilder().setSubscriptionId(subscriptionId).setRequestedDate(now).setEffectiveDate(effectiveDate).setActiveVersion(activeVersion).setPhaseName(phaseName));
}
