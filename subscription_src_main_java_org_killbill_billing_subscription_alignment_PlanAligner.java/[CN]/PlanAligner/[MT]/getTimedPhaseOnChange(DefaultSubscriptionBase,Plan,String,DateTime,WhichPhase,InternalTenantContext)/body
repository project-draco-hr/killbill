{
  return getTimedPhaseOnChange(subscription.getAlignStartDate(),subscription.getBundleStartDate(),subscription.getCurrentPhase(),subscription.getCurrentPlan(),subscription.getCurrentPriceList().getName(),nextPlan,nextPriceList,effectiveDate,effectiveDate,subscription.getAllTransitions().get(0).getNextPhase().getPhaseType(),which,context);
}
