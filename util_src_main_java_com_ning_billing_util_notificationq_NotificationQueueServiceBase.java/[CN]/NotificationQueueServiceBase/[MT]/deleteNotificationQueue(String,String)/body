{
  String compositeName=getCompositeName(svcName,queueName);
synchronized (queues) {
    NotificationQueue result=queues.get(compositeName);
    if (result == null) {
      throw new NoSuchNotificationQueue(String.format("Queue for svc %s and name %s does not exist",svcName,queueName));
    }
    queues.remove(compositeName);
  }
}
