{
  final Collection<String> ddlFiles=new LinkedList<String>();
  for (  final String module : new String[]{"account","beatrix","entitlement","invoice","payment","subscription","tenant","usage","util","server"}) {
    ddlFiles.add("org/killbill/billing/" + module + "/ddl.sql");
  }
  return ddlFiles;
}
