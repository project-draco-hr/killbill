{
  return invoiceItem.getSubscriptionId().equals(repairedSubscriptionId) && repairInvoiceItem.getStartDate().compareTo(invoiceItem.getStartDate()) <= 0 && (invoiceItem.getEndDate() != null && repairInvoiceItem.getEndDate().compareTo(invoiceItem.getEndDate()) >= 0);
}
