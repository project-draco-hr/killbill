{
  if (cases != null) {
    for (int i=cases.length - 1; i >= 0; i--) {
      K result=cases[i].getResult(planSpec,catalog);
      if (result != null) {
        return result;
      }
    }
  }
  return null;
}
