{
  account=new BusinessAccount(UUID.randomUUID(),ACCOUNT_KEY,UUID.randomUUID().toString(),BigDecimal.ONE,clock.getUTCToday(),BigDecimal.TEN,"ERROR_NOT_ENOUGH_FUNDS","CreditCard","Visa","FRANCE",CURRENCY,clock.getUTCNow(),clock.getUTCNow());
  final IDBI dbi=helper.getDBI();
  businessAccountSqlDao=dbi.onDemand(BusinessAccountSqlDao.class);
  try {
    businessAccountSqlDao.test(internalCallContext);
  }
 catch (  Throwable t) {
    Assert.fail(t.toString());
  }
}
