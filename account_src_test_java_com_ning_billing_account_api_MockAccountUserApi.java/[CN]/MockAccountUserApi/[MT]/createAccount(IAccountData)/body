{
  IAccount result=new Account().withKey(data.getKey()).withName(data.getName()).withEmail(data.getEmail()).withPhone(data.getPhone()).withBillCycleDay(data.getBillCycleDay()).withCurrency(data.getCurrency());
  accounts.add(result);
  return result;
}
