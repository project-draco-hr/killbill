{
  transactional.deleteByAccountRecordId(BusinessBundleSummaryModelDao.BUNDLE_SUMMARIES_TABLE_NAME,accountRecordId,tenantRecordId,context);
  for (  final BusinessBundleSummaryModelDao bbs : bbss) {
    transactional.create(bbs.getTableName(),bbs,context);
  }
}
