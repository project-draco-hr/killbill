{
  if (!isInitialized) {
    try {
      catalog=CatalogLoader.getCatalogFromProperty(config.getCatalogURI());
      isInitialized=true;
    }
 catch (    Exception e) {
      throw new ServiceException(e);
    }
  }
}
